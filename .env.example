# LLM Provider Configuration
# Options: "ollama" (local, free) or "anthropic" (cloud, paid)
LLM_PROVIDER=ollama

# Ollama Configuration (for local LLM)
# Make sure Ollama is running: ollama serve
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=phi4-mini:latest  # Smaller models: phi4-mini:latest (2.5GB), llama3.2:3b (1.9GB), qwen3:8b (5.2GB)

# Anthropic Claude API (for cloud LLM)
# Get your key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-5

# Optional: Semantic Scholar API (free, but rate-limited without key)
# SEMANTIC_SCHOLAR_API_KEY=

# Optional: Enable debug logging
DEBUG=false
