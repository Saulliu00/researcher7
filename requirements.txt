# Core Dependencies
python-dotenv>=1.0.0
requests>=2.31.0

# LLM Providers (optional based on configuration)
anthropic>=0.18.0

# SSL fix for corporate proxies
truststore>=0.9.0

# NLP & ML
scikit-learn>=1.4.0
numpy>=1.24.0

# Academic Paper Search
semanticscholar>=0.8.3
arxiv>=2.1.0

# Translation (install when using --lang zh with --translator marianmt)
transformers>=4.36.0
sentencepiece>=0.1.99
torch>=2.1.0

# TTS - Piper runs as a standalone binary, no pip package needed.
# Download piper + voice models from:
#   https://github.com/rhasspy/piper/releases
#   https://huggingface.co/rhasspy/piper-voices
# Then set PIPER_EXE and PIPER_VOICE in .env
